---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "seekrit_environment Resource - seekrit"
subcategory: ""
description: |-
  A seekrit environment, owned by exactly one of an application or a group. On create the provider generates the environment DEK and wraps it to the configured service token, so that token can immediately manage the environment's secrets. No secret plaintext is involved.
---

# seekrit_environment (Resource)

A seekrit environment, owned by exactly one of an application or a group. On create the provider generates the environment DEK and wraps it to the configured service token, so that token can immediately manage the environment's secrets. No secret plaintext is involved.

## Example Usage

```terraform
# An environment is owned by exactly one of an application or a group.
resource "seekrit_environment" "production" {
  application_id = seekrit_application.web.id
  name           = "Production"
  slug           = "production"
}

# The group-side environment. Composition matches on slug, so the group
# environment a `production` app environment inherits is the one also called
# `production`.
resource "seekrit_environment" "observability_production" {
  group_id = seekrit_group.observability.id
  name     = "Production"
  slug     = "production"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Human-readable environment name (editable in place).
- `slug` (String) URL-safe identifier, unique per owner and the key group composition matches on. Immutable — changing it replaces the environment.

### Optional

- `application_id` (String) The owning application id. Exactly one of `application_id` or `group_id` must be set. Immutable — changing ownership replaces the environment.
- `group_id` (String) The owning group id. Exactly one of `application_id` or `group_id` must be set. Immutable — changing ownership replaces the environment.

### Read-Only

- `created_at` (String) Creation timestamp (ISO 8601).
- `id` (String) Server-generated environment id (`env_…`).
- `updated_at` (String) Last-update timestamp (ISO 8601).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import seekrit_environment.production env_9QpZ3vLmKd8
```
