---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "seekrit_organization Data Source - seekrit"
subcategory: ""
description: |-
  The organization the configured token belongs to. Takes no arguments — a service token is scoped to exactly one org, so there is nothing to look up. Useful for asserting a configuration is pointed at the org you think it is, and for reading back role to confirm the token really is an admin.
---

# seekrit_organization (Data Source)

The organization the configured token belongs to. Takes no arguments — a service token is scoped to exactly one org, so there is nothing to look up. Useful for asserting a configuration is pointed at the org you think it is, and for reading back `role` to confirm the token really is an admin.

## Example Usage

```terraform
# No arguments: a service token belongs to exactly one org, so there is nothing
# to look up. Useful as a guard that a configuration is pointed where you think.
data "seekrit_organization" "current" {}

check "provider_credential_is_admin" {
  assert {
    condition     = data.seekrit_organization.current.role == "admin"
    error_message = "SEEKRIT_TOKEN is a member token; creating applications and minting tokens needs an admin token."
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `id` (String) The organization id (`org_…`).
- `name` (String) Display name.
- `role` (String) The configured token's role in this org (`admin` or `member`). Most resources in this provider need `admin`.
- `slug` (String) URL-safe identifier, globally unique.
